<image class="image-class captcha-img" wx:if="{{url}}" src="{{url}}" mode="{{mode}}" bindload="handleLoaded" binderror="handleError" style="{{ imgStyle + (autoWidth ? 'width: ' + width + 'px;' : '') }}"></image>